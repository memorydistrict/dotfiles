--+=+----------------------------------------+=+--
--+=+-: User.Config.Keymap.Tools.Telescope :-+=+--
--+=+----------------------------------------+=+--

return {
    move_selection = {
        prev = [[<C-k>]],
        next = [[<C-j>]],
    },
    quickfix_list = {
        open_ui = [[<C-q>]],
        send_selected = [[<C-q>]],
    },
    delete_buffer = [[<C-x>]],
}
